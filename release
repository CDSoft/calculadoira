if [ -z "$1" ]
then
    echo "Usage: $0 version"
    exit -1
fi
if true # make all tests
then
    mkdir -p dist
    git tag v$1
    git archive --format=tar --prefix=calculadoira-v$1/ v$1 | gzip -9 > dist/calculadoira-v$1.tgz
fi
